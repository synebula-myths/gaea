publishing {
    publications {
        publish(MavenPublication) {
            group "${project.group}"
            artifactId "${project.name}"
            version "$version"
            from components.java
        }
    }
}