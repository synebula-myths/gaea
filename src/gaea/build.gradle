publishing {
    publications {
        publish(MavenPublication) {
            group 'com.synebula'
            artifactId 'gaea'
            version "$version"
            from components.java
        }
    }
}