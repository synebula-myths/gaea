publishing {
    publications {
        mavenJava(MavenPublication) {
            group 'com.synebula'
            artifactId 'gaea.data'
            version project.version
            from components.java
        }
    }
}
